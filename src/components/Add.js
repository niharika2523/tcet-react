function Add(props){
    return (
        <>
            <h1>
                This is add 
            </h1>
        
            <h3>Addition of {props.x} and {props.y} is {props.x + props.y} </h3>
        </>
    )
    
}

export default Add;